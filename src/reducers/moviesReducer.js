const initialState = {

}

 const moviesReducer = (state = [], { type, payload }) => {
    switch (type) {

    // case typeName:
    //     return { ...state, ...payload }

    default:
        return state
    }
}
export default moviesReducer